/**
 * Default hotkey for toggling line-break. (enter)
 *
 * @remarks
 * If the hotkey is `enter`, and so on.
 *
 * @see [is-hotkey]{@link https://www.npmjs.com/package/is-hotkey}
 */
export const LINE_BREAK_HOTKEY = 'enter';

/**
 * Default hotkey for toggling line-break. (shift + enter)
 *
 * @remarks
 * If the hotkey is `shift+enter`, and so on.
 *
 * @see [is-hotkey]{@link https://www.npmjs.com/package/is-hotkey}
 */
export const LINE_BREAK_SHIFT_HOTKEY = 'shift+enter';

/**
 * Default hotkey for initial line-break position.
 *
 * @remarks
 * If the hotkey is `Backspace` | `Meta+Backspace` | 'ctrl+Backspace', and so on.
 *
 * @see [is-hotkey]{@link https://www.npmjs.com/package/is-hotkey}
 */
export const LINE_BREAK_INITIAL_HOT_KEYS = ['Backspace', 'Meta+Backspace', 'ctrl+Backspace'];
